# 复制本文件为 .env，放到 ~/.claude/skills/.env，填入你自己的火山引擎 API Key。
# 真实 .env 已被 .gitignore 忽略，不会进仓库。
#
# 申请地址（新版控制台，生成「一个」API Key 即可，所有引擎共用）：
#   https://console.volcengine.com/speech/new/setting/apikeys
#
# 还需在控制台开通两个资源（各有 20h 免费额度，独立抵扣）：
#   · 录音文件识别 - 极速版  (volc.bigasr.auc_turbo)
#   · 录音文件识别 - 标准版  (volc.bigasr.auc)
# 默认 auto 模式两者轮流，吃满 ≈40h；只开一个就用 --flash 或 --v3-standard。

VOLCENGINE_API_KEY=your_api_key_here
